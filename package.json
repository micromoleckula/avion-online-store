{
  "name": "avion-online-store",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "sass": "^1.86.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "firebase": "^11.5.0",
    "json-server": "^1.0.0-beta.3",
    "normalize.css": "^8.0.1"
  }
}
